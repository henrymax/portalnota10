# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  encoding: utf8
  adapter: mysql
  database: portalnota10
  host: localhost
  username: root
  password:
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlite3
  database: db/test.sqlite3
  pool: 5
  timeout: 5000

production:
  encoding: utf8
  adapter: mysql
  database: portalnota10
  host: mysql.portalnota10.com.br
  username: portalnota10
  password: ptlptl
  timeout: 5000
